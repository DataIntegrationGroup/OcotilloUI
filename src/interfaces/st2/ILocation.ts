import { IBaseSTEntity } from "@/interfaces/st2";

export interface ILocation extends IBaseSTEntity {
  // "@iot.id" : string;
  // name: string;
  // properties: any;
  // PointID: string;
  // SiteID: string;
  // PublicRelease: boolean;
  // geometry: { coordinates: number[], type: string };
  // Easting: number;
  // Northing: number;
  // site_type: string;
  // title: string;
  // status: IStatus;
  // category: ICategory;
}
